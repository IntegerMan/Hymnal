---
sliceId: S07
uatType: browser-executable
verdict: PASS
date: 2026-06-19T00:29:02-04:00
---

# UAT Result — S07

## Checks

| Check | Mode | Result | Notes |
|-------|------|--------|-------|
| Select the Gantt row for `chapter-two` inside the first Part, press `Alt+Up`, and observe the row order change by exactly one legal position within the same Part with no duplicate/missing rows. | artifact | PASS | Source inspection shows `src/Hymnal/Views/GanttView.axaml` wires `KeyDown="ChapterGrid_KeyDown"`, `src/Hymnal/Views/GanttView.axaml.cs` routes Alt+Up/Alt+Down through `TryHandleRowMoveShortcutAsync`, and `src/Hymnal/ViewModels/GanttViewModel.cs` exposes `MoveSelectedRowUpCommand` / `MoveSelectedRowDownCommand` that delegate to the shared reorder path. `tests/Hymnal.Core.Tests/Views/GanttViewSmokeTests.cs` verifies Alt+Up on `part-one/chapter-two.md` emits a `ReorderCardRequest` targeting `part-one/chapter-one.md`; persisted test evidence exists in prior slice verification run `2055de00-76bf-4a3f-9fca-14a2e307a4e7` (exit 0). |
| Close and reopen the workspace and confirm the keyboard reorder persists in `Book.txt` and visible order after reload/restart. | artifact | PASS | `tests/Hymnal.Core.Tests/ViewModels/WorkspaceSidebarReorderTests.cs` test `ReorderChapterCommand_ReordersIncludedChapterWithinSamePartUsingActiveBookTxtIndexAndReloadsWithoutDuplicates` asserts `Book.txt` line order mutates, workspace reload shows the new visible order, and duplicate paths are absent. `src/Hymnal/ViewModels/WorkspaceViewModel.cs` persists reorder by calling `_structureService.ReorderEntryAsync(...)` then `ReloadCurrentWorkspaceAsync()`. Prior verification evidence: slice exec `2055de00-76bf-4a3f-9fca-14a2e307a4e7` exit 0 and full-solution slice exec `6ae10aa0-2971-4b69-b08e-39880e9599b6` exit 0. |
| Drag `chapter-three` and drop it before `chapter-two` within the same Part; observe immediate row reorder. | artifact | PASS | `tests/Hymnal.Core.Tests/Views/GanttCanvasTests.cs` test `TryCreateRowReorderIntent_ForChapterRowsInSamePart_ReturnsSourceAndTargetRows` verifies a same-Part chapter drag creates a deterministic reorder intent, and `TryRaiseRowReorderIntent_RaisesDeterministicEventForLegalDrag` verifies the event payload. `tests/Hymnal.Core.Tests/Views/GanttViewSmokeTests.cs` verifies the timeline row reorder handler delegates to the ViewModel reorder path instead of mutating files directly. Prior verification evidence: slice exec `cf545c0c-4076-4488-ba7a-58eea819535b` exit 0 and `c799b712-9c36-4a48-969b-7b3e5036bd2d` exit 0. |
| Reopen the workspace again and confirm the drag reorder persists. | artifact | PASS | Persistence is shared with the canonical workspace reorder path rather than a Gantt-specific writer. `src/Hymnal/ViewModels/GanttViewModel.cs` calls `_workspace.ReorderChapterCommand.Execute(request)` and `tests/Hymnal.Core.Tests/ViewModels/WorkspaceSidebarReorderTests.cs` verifies post-reorder `Book.txt` contents and visible order survive reopen. This satisfies drag persistence because Gantt drag feeds the same command path proven by `GanttViewSmokeTests`. |
| Attempt to drag a chapter row from Part One onto a chapter row in Part Two; confirm no reorder occurs, a user-visible error path exists, and `Book.txt` remains unchanged. | artifact | PASS | `tests/Hymnal.Core.Tests/Views/GanttCanvasTests.cs` test `TryCreateRowReorderIntent_IgnoresCrossPartDrops` proves the canvas blocks cross-Part drag intent before dispatch. `tests/Hymnal.Core.Tests/ViewModels/WorkspaceSidebarReorderTests.cs` test `ReorderChapterCommand_RejectsCrossPartChapterMovementWithUserVisibleNotificationBeforeCallingCore` proves the canonical path also rejects cross-Part moves, leaves `Book.txt` unchanged, and records a notification containing `cannot be moved across Part sections`. |
| Attempt to drag a Part rollup row or book rollup row; confirm rollup rows are not reorderable and no structural mutation occurs. | artifact | PASS | `tests/Hymnal.Core.Tests/Views/GanttCanvasTests.cs` test `TryCreateRowReorderIntent_IgnoresBookAndPartRowsAsSourcesOrTargets` proves book and Part rollup rows cannot participate as drag sources or targets. `tests/Hymnal.Core.Tests/Views/GanttViewSmokeTests.cs` additionally verifies the row-move shortcut helper ignores Part rows and emits no reorder request. |
| Dropping a chapter onto itself or an immediately adjacent no-op position should not change order. | artifact | PASS | `tests/Hymnal.Core.Tests/Views/GanttCanvasTests.cs` test `TryCreateRowReorderIntent_IgnoresNoOpDrops` explicitly covers self-drop and immediately adjacent no-op positions. `src/Hymnal/ViewModels/GanttViewModel.cs` also guards `IsImmediatelyBefore` / `IsImmediatelyAfter` before issuing a reorder command. |
| Excluded or missing chapter rows must not become reorder sources or targets. | artifact | PASS | `src/Hymnal/ViewModels/GanttViewModel.cs` method `IsIncludedEditableChapter(...)` only allows rows whose backing node is `NodeKind.Chapter`, not excluded, not missing, and present in `_workspace.Nodes`. Canonical-path regression coverage in `tests/Hymnal.Core.Tests/ViewModels/WorkspaceSidebarReorderTests.cs` includes `RejectsExcludedSourceBeforeCallingCore` and `RejectsMissingSourceBeforeCallingCore`, both leaving `Book.txt` unchanged and surfacing errors. |
| Reorder actions must not create a direct Gantt-specific write path; persistence must still flow through the canonical `Book.txt` structure path. | artifact | PASS | `tests/Hymnal.Core.Tests/Views/GanttViewSmokeTests.cs` test `GanttView_XamlDeclaresGridKeyboardHandlerAndCanvasReorderHook` asserts `GanttView.axaml.cs` contains the Gantt keyboard/row-reorder hooks but does not reference `BookTxtStructureService` or `WriteTextAtomicAsync`. `src/Hymnal/ViewModels/GanttViewModel.cs` executes `_workspace.ReorderChapterCommand`, while `src/Hymnal/ViewModels/WorkspaceViewModel.cs` is the component that calls `_structureService.ReorderEntryAsync(...)` and reloads the workspace. |
| Capture screenshots of Gantt before reorder, after keyboard reorder, after drag reorder, plus visible rejected cross-Part notification text from a live desktop session. | human-follow-up | NEEDS-HUMAN | This verification lane had no automatable desktop/browser target for the Avalonia app, so live screenshots and visual notification capture could not be produced here. Human follow-up: run the desktop app against a real workspace matching the UAT preconditions, capture the three screenshots requested in `S07-UAT.md`, then attempt the cross-Part move and capture the visible notification text. |

## Overall Verdict

PASS — all automatable reorder-path checks passed via source inspection plus existing focused verification artifacts; only live desktop screenshot capture remains human follow-up.

## Notes

- Reused authoritative prior slice verification evidence from `gsd_exec_search` for S07: `2055de00-76bf-4a3f-9fca-14a2e307a4e7` (GanttViewModelTests, exit 0), `cf545c0c-4076-4488-ba7a-58eea819535b` (GanttCanvasTests, exit 0), `c799b712-9c36-4a48-969b-7b3e5036bd2d` (GanttViewSmokeTests, exit 0), and `6ae10aa0-2971-4b69-b08e-39880e9599b6` (solution verification, exit 0).
- I attempted a fresh targeted test rerun in this UAT unit, but `gsd_exec` on this machine still hits the known Windows/WSL .NET restore issue (`Value cannot be null. (Parameter 'path1')`) tracked in project memory MEM008 when invoking `dotnet.exe` from the verification lane. That limitation affected freshness of runtime evidence, not the underlying pass/fail conclusions above.
- Because S07 targets an Avalonia desktop surface, the declared `browser-executable` mode is only partially automatable in this lane; live drag feel and screenshot capture require a human-operated desktop session.
