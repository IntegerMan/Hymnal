# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | UI framework for cross-platform desktop | Avalonia UI 12.0 with Skia renderer + ReactiveUI MVVM + DynamicData | Desktop-first, MIT licensed, Linux first-class, Skia gives pixel-level control for custom Gantt/Corkboard rendering. ReactiveUI + DynamicData are a designed pair with first-class integration. Production use at JetBrains, Devolutions, NASA. Uno Platform rejected: mobile/web breadth unused; commercial tooling tier creates friction for solo MIT project. | no — would require rewriting all views | collaborative |
| D002 |  | architecture | Project structure and layer boundary enforcement | Two-project solution: src/Hymnal/ (Avalonia UI) + src/Hymnal.Core/ (pure .NET 10, zero Avalonia reference); compile-enforced via csproj with no Avalonia package reference in Core | Compile errors are stronger than code-review notes. Any agent that imports Avalonia types into a service gets a build failure, not a convention violation. Tests in Hymnal.Core.Tests have zero UI dependency. | no — foundational to testability | collaborative |
| D003 |  | architecture | Metadata storage format | JSON files in .hymnal-data/ using System.Text.Json; schemaVersion in every file; camelCase properties; ISO 8601 dates; atomic writes (write-temp-then-rename) | Human-readable, Git-diffable, inspectable and scriptable outside the app — consistent with Hymnal's plaintext/Git philosophy. LeanQuill (real parallel implementation) confirmed schema versioning is non-negotiable after a painful schema_version migration. | partially — SQLite migration path noted for post-V1 multi-author scope | collaborative |
| D004 |  | pattern | Error handling pattern for all async service calls | Result&lt;T&gt; readonly record struct in Hymnal.Core/Common/; all async service methods return Task&lt;Result&lt;T&gt;&gt;; ViewModels surface failures via INotificationService.ShowError() | Errors propagate as values, not exceptions; forces explicit handling at the ViewModel boundary; prevents silent swallowing. ReactiveCommand.ThrownExceptions subscribed as safety net for unexpected throws. | no — pervasive, changing it would require rewriting all service signatures | agent |
| D005 |  | library | Editor component | AvaloniaEdit TextEditor with custom XSHD IHighlightingDefinition for Markua 0.30 | Purpose-built code editor for Avalonia; supports custom syntax definitions, AbstractMargin for inline issue indicators (FR-50, M005), and integrates with ReactiveUI binding model. Plain TextBox has no syntax extension point. | low — would require replacing editor surface in M001 | agent |
| D006 |  | architecture | AI provider abstraction | Microsoft.Extensions.AI IChatClient as the internal abstraction; LiteLLM endpoint via OpenAIChatClient (MEAI) or native MEAI/Agent Framework implementation; API key in OS credential store only | IChatClient provides a single interface regardless of provider; LiteLLM compatibility allows any OpenAI-compatible endpoint. AiEditorialService returns data only — all persistence is caller-controlled (learned from LeanQuill's manuscript_write_blocked pattern). | yes — provider can be swapped behind IChatClient without changing callers | collaborative |
| D007 |  | architecture | Git integration approach | ProcessGitService: invoke system git binary via System.Diagnostics.Process and PATH; no bundled libgit2/LibGit2Sharp | Simpler; no binary bundling; consistent with Hymnal's philosophy of meeting authors where they already are (they already have git installed). Stage-all→commit→push is the only workflow; full libgit2 API surface is unused. | yes — LibGit2Sharp could be added if richer Git features are needed post-V1 | agent |
| D008 |  | architecture | Where to place AppSettingsStore implementation and IFolderPickerService interface seam | AppSettingsStore goes in src/Hymnal.Core/Infrastructure/ (zero Avalonia deps — only System.Text.Json + System.IO); IFolderPickerService interface goes in Hymnal.Core.Interfaces returning Task&lt;string?&gt;; FolderPickerService (Avalonia impl) goes in src/Hymnal/Infrastructure/ | AppSettingsStore has no Avalonia types so it can live in Core, making it directly testable from Hymnal.Core.Tests without adding a project reference to Hymnal (which would pull Avalonia into the test runner). IFolderPickerService returns a plain string? path keeping Core clean; the Avalonia IStorageProvider call is isolated in FolderPickerService in the UI project, preserving the established Core/UI compile boundary. | yes — if a future milestone needs a richer settings model, move AppSettingsStore to Hymnal and add a Hymnal.Tests project | agent |
| D009 |  | architecture | How to let WorkspaceViewModel trigger a folder picker without taking an Avalonia dependency in Core | Thin IFolderPickerService interface in Hymnal.Core.Interfaces; FolderPickerService implementation in Hymnal wraps Avalonia IStorageProvider via TopLevel.GetTopLevel() | Hymnal.Core must have zero Avalonia package references (enforced compile boundary from D001). A dedicated service interface keeps the ViewModel testable and keeps the Core/UI boundary clean. The factory lambda `Func<TopLevel?>` passed to FolderPickerService at registration time avoids needing a direct MainWindow reference in DI. | yes — could use a messenger/dialog abstraction instead, but this is the minimum-complexity solution | agent |
| D010 | S03 planning | architecture | Where to store manuscript workspace root paths for downstream chapter resolution | Add WorkspaceRoot and ManuscriptRoot string properties to ManuscriptModel, set by ManuscriptService after successful Book.txt parse | ManuscriptService already computes manuscriptRoot when finding Book.txt (handling both root and manuscript/ layouts) but discards it before returning. Without root path in the model, EditorViewModel cannot resolve absolute chapter file paths for load/save/watch. Storing roots in ManuscriptModel keeps path ownership centralized in the model that every downstream consumer already holds. | Yes | agent |
| D011 | S03 planning | architecture | Placement of IMetadataStore atomic-write abstraction | IMetadataStore in Hymnal.Core.Interfaces, MetadataStore in Hymnal.Core.Infrastructure — same package pattern as IAppSettingsStore/AppSettingsStore | Chapter saves (S03) and chapter notes (S04) both need atomic text-file writes. Placing the interface and implementation in Core keeps the Avalonia shell free of file-I/O logic, makes the store unit-testable without the UI layer, and matches the established AppSettingsStore pattern already in Core. | Yes | agent |
| D012 | S03 planning | architecture | How to surface the external-file-change conflict choice to the user | Editor-local conflict strip (visible Border in EditorView with HasConflict/ConflictMessage/AcceptExternalCommand/KeepLocalCommand on EditorViewModel) rather than extending INotificationService with an action model | INotificationService only supports fire-and-forget string messages with no dismiss state or action callbacks. Adding an action model would require protocol changes that ripple across the notification infrastructure. An editor-local conflict strip owned entirely by EditorViewModel/EditorView scopes the complexity correctly and does not over-generalize the notification contract before it is needed elsewhere. | Yes | agent |
| D013 | S03 planning | architecture | Implementation approach for global notification banner UI (currently debug-log-only) | Notification banner in MainWindow as a DockPanel.Dock=Bottom Border bound to HasBanner/BannerMessage/BannerKind properties on MainWindowViewModel; MainWindowViewModel subscribes to NotificationService.Notifications and auto-clears after 5 seconds via Observable.Timer | NotificationService already exposes IObservable<Notification>. Routing through MainWindowViewModel keeps banner state bindable via standard AXAML compiled bindings, avoids code-behind subscriptions that bypass the VM layer, and keeps the notification service interface unchanged while satisfying the S03 requirement to surface save failures visibly. | Yes | agent |
| D014 | M002/S04/T02 override alignment | architecture | Reactive state wiring for ChapterInfoViewModel target and proximity indicators | Treat the user-provided ChapterInfoViewModel implementation as canonical: keep ProximityFill and HasTarget as plain backing-field properties updated from live ChapterViewModel subscriptions rather than ObservableAsPropertyHelper chains. | The override removes the startup null-initialization hazard from self-referential OAPH ordering, keeps Chapter Info synchronized with the same live target/proximity state used elsewhere in the UI, and gives S04/T02 a concrete authoritative implementation to verify against the recovered M002 working tree. | Yes | human |
| D015 | M003 | architecture | How to render the Gantt timeline view in Avalonia 12. | Build a custom Avalonia control (`GanttCanvas`) overriding `Render(DrawingContext)` rather than composing standard Avalonia UI elements like Grids and Borders. | A custom `DrawingContext` renderer provides full control over the time-axis scaling and high performance for rendering hundreds of phase boxes, which can become sluggish if using a heavy visual tree composed of individual UI controls. This matches the known architectural complexity risk for M003. | Yes | agent |
| D016 | M003/S03 follow-up | architecture | Center-panel navigation should use explicit shell modes instead of a binary Gantt toggle | MainWindowViewModel now exposes ShellMode with Write and Manage active, Plan and Edit reserved, with Write as the default mode and Manage hosting the Gantt surface. | The requested top navigation is mode-based, not feature-toggle-based. An explicit mode model keeps the shell semantics aligned with the UI labels, makes the default state obvious, and preserves the existing menu/hotkey behavior by mapping it onto the same source of truth. | Yes | agent |
| D017 | M004 planning | planning | M004 roadmap decomposition | Use three vertical slices aligned to the three primary M004 requirements: S01 Corkboard, S02 Supplemental Docs, S03 Git toolbar workflow. | The milestone consists of three user-visible Early V1 capabilities with clear acceptance boundaries. Keeping one primary requirement per slice preserves traceability, supports demoable completion after each slice, and avoids over-splitting Git or docs into foundation-only work. | Yes | agent |
| D018 | M004 planning | architecture | Supplemental docs tree representation | Model supplemental docs with a separate docs tree model rather than extending ChapterNode or NodeKind. | ChapterNode and its converters are manuscript-specific and already distinguish Part versus Chapter behavior. A separate docs tree keeps Book.txt/manuscript semantics stable while still allowing docs to open through the existing EditorViewModel file lifecycle. | Yes | agent |
| D019 | M004/S01 planning | corkboard-structural-editing | S01 include and exclude semantics | Treat Exclude as removing the chapter path from Book.txt while leaving the chapter file on disk; Include means adding an existing or newly created manuscript file path back into Book.txt via explicit Plan-mode actions. | Book.txt is the authoritative manuscript order and existing registry logic already preserves orphaned chapter identity when paths disappear. This avoids inventing comment syntax that the parser does not support and keeps file deletion separate from a reversible manuscript-structure action. | Yes | agent |
| D020 | M004/S01 planning | corkboard-structural-editing | S01 cross-Part drag behavior | Drag reorder changes Book.txt ordering only and does not physically move chapter files between folders. | Moving files across folders would turn a visual reorder into a rename/file relocation with watcher, registry, and link-breaking risks. Book.txt can already express arbitrary ordering across Part dividers, so order-only moves satisfy the corkboard restructuring contract with lower data-loss risk. | Yes | agent |
| D021 | M004/S01 planning | corkboard-structural-editing | S01 remove versus delete behavior | Expose non-destructive Remove from Book separately from destructive Delete chapter file; only Delete removes the markdown file and must require confirmation. | Authors need a low-friction way to exclude a chapter from the manuscript without data loss, while file deletion is irreversible enough to require explicit confirmation and raw error reporting if the filesystem operation fails. | Yes | agent |
| D022 | M005 plan-milestone | milestone-planning | M005 slice order | Plan risk-first with S01 atomic structure core before sidebar, Corkboard, and Gantt UI slices, followed by integrated consistency UAT in S08. | The highest data-loss risk is the multi-resource boundary between Book.txt, chapter file moves, exclusions, and registry continuity; proving that contract first lets every UI surface remain a thin consumer and avoids three divergent structural implementations. | Yes | agent |
| D023 | M005 plan-milestone | architecture | Canonical structural edit path for M005 | Route sidebar, Corkboard, and Gantt structural operations through the Core BookTxtStructureService and WorkspaceViewModel reload orchestration rather than direct View or ViewModel file writes. | A single structural path preserves atomic-write guarantees, watcher suppression behavior, registry reconciliation, and consistent restart persistence across all manuscript structure surfaces. | Yes | agent |
| D024 | M005/S01 plan-slice | architecture | M005/S01 exclusion manifest ownership | Add a Core exclusion manifest service backed by .hymnal-data/exclusions.json, with BookTxtStructureService consuming it for intentional include/exclude operations rather than making orphan discovery persist exclusions. | Exclusions are user intent, not every markdown orphan. Keeping manifest persistence as a small Core service gives later UI surfaces a single load/save contract while preserving Book.txt as the authoritative ordered include list. | Yes | agent |
| D025 | M005/S01 plan-slice | architecture | M005/S01 path-changing structure API | Expose a dedicated single-operation Core method for path-changing chapter moves that coordinates target conflict checks, file move, Book.txt rewrite, registry UUID continuity, and rollback reporting. | Later sidebar and Corkboard surfaces need one user action with one Result, not a composition of rename, reorder, and registry calls that can diverge or hide partial failure. A dedicated API makes failure phases and rollback semantics testable in Core. | Yes | agent |
